# Copy this file to .env.local (dev), or upload it as .env.test / .env.production
# on each droplet. NEVER commit real values: only this example file goes to Git.

NODE_ENV=development
APP_ENV=development
APP_URL=http://localhost:3001
API_URL=http://localhost:8080

